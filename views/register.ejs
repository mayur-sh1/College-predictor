<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sign Up | RankToCollege</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
  <style>
    :root {
      --primary: #2563eb;
      --primary-dark: #1e40af;
      --secondary: #f1f5f9;
      --white: #ffffff;
      --text: #1e293b;
      --muted: #64748b;
      --shadow: 0 4px 24px rgba(30, 64, 175, 0.08);
      --radius: 0.8rem;
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: 'Inter', sans-serif;
      background: var(--secondary);
      color: var(--text);
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
    }

    .signup-container {
      background: var(--white);
      box-shadow: var(--shadow);
      border-radius: var(--radius);
      padding: 2.5rem;
      max-width: 400px;
      width: 100%;
    }

    h2 {
      margin-bottom: 0.5rem;
      font-size: 1.75rem;
      font-weight: 700;
    }

    p.subtext {
      color: var(--muted);
      margin-bottom: 1.5rem;
      font-size: 0.95rem;
    }

    .google-btn {
      width: 100%;
      border: 1px solid #e2e8f0;
      padding: 0.7rem;
      border-radius: var(--radius);
      display: flex;
      justify-content: center;
      align-items: center;
      gap: 0.5rem;
      font-weight: 600;
      cursor: pointer;
      background: var(--white);
    }

    .divider {
      display: flex;
      align-items: center;
      margin: 1.5rem 0;
      color: var(--muted);
      font-size: 0.85rem;
    }

    .divider::before,
    .divider::after {
      content: "";
      flex: 1;
      height: 1px;
      background: #e2e8f0;
    }

    .divider::before {
      margin-right: 0.5rem;
    }

    .divider::after {
      margin-left: 0.5rem;
    }

    label {
      font-weight: 600;
      display: block;
      margin-bottom: 0.3rem;
    }

    input[type="text"],
    input[type="email"],
    input[type="password"] {
      width: 100%;
      padding: 0.6rem 0.75rem;
      margin-bottom: 1.2rem;
      border-radius: var(--radius);
      border: 1px solid #cbd5e1;
      font-size: 1rem;
    }

    .submit-btn {
      background: var(--text);
      color: var(--white);
      border: none;
      width: 100%;
      padding: 0.75rem;
      font-size: 1rem;
      border-radius: var(--radius);
      font-weight: 600;
      cursor: pointer;
    }

    .submit-btn:hover {
      background: var(--primary);
    }

    .footer-text {
      text-align: center;
      margin-top: 1rem;
      font-size: 0.95rem;
    }

    .footer-text a {
      color: var(--primary);
      text-decoration: none;
      font-weight: 600;
    }

    .footer-text a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="signup-container">
    <h2>Sign up</h2>
    <p class="subtext">Choose your preferred sign up method</p>
    <button class="google-btn">
      <img src="https://www.svgrepo.com/show/475656/google-color.svg" alt="Google logo" width="18" height="18">
      Google
    </button>

    <div class="divider">OR CONTINUE WITH</div>

    <form>
      <label for="name">Name</label>
      <input type="text" id="name" name="name" placeholder="Your name" required>

      <label for="email">Email</label>
      <input type="email" id="email" name="email" placeholder="Enter your email" required>

      <label for="password">Password</label>
      <input type="password" id="password" name="password" placeholder="" required>

      <button type="submit" class="submit-btn">Sign Up</button>
    </form>

    <div class="footer-text">
      Already have an account? <a href="signin.html">Sign in</a>
    </div>
  </div>
</body>
</html>